# Revisions listed here are ignored by `git blame` when run with
# `--ignore-revs-file=.git-blame-ignore-revs`. Configure once per
# checkout:
#
#   git config blame.ignoreRevsFile .git-blame-ignore-revs
#
# Add commits whose ONLY effect is whitespace / formatting churn.

# clang-format sweep — initial introduction of .clang-format
2f45fbf
