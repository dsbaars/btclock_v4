# BTClock V8 prototype — 16 MB flash (0x1000000)
# Layout matches production partition_16mb.csv. Both app offsets are
# 64 KiB-aligned. LittleFS stays at 2 MiB as in production. Fully
# uses the 16 MiB flash with no tail waste.
#
# "storage" uses the `littlefs` subtype (vs `spiffs` in production);
# esp_littlefs mounts by partition label either way. Scope asked for
# ~4 MiB on the V8 board; bumping from 2 MiB -> 4 MiB would require
# shrinking both app slots by ~1 MiB each (losing 1 MiB of OTA
# headroom), which is a separate decision — keep 2 MiB here.
#
# Name,    Type, SubType,  Offset,   Size,     Flags
nvs,       data, nvs,      0x9000,   0x5000
otadata,   data, ota,      0xe000,   0x2000
app0,      app,  ota_0,    0x10000,  0x6F0000
app1,      app,  ota_1,    0x700000, 0x6F0000
storage,   data, littlefs, 0xDF0000, 0x200000
coredump,  data, coredump, 0xFF0000, 0x10000
